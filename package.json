{
  "name": "quran-reader",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build --base=./ && node scripts/prepare-sites-build.mjs",
    "dev": "vite --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^6.4.3"
  }
}
